
# Auto-detect text files and ensure consistent line endings
* text=auto

# Force LF for Python files (recommended for cross-platform compatibility)
*.py text eol=lf

# Force LF for shell scripts
*.sh text eol=lf

# Retain CRLF for batch scripts (Windows-only)
*.bat text eol=crlf

# Normalize common text formats
*.md text
*.txt text
*.json text
*.yml text
*.yaml text
*.sql text
*.ini text
*.cfg text
